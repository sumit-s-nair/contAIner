{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "IntentDatasetRow",
  "description": "Schema for a single row in the intent-dataset",
  "type": "object",
  "required": ["instruction", "intent_type", "entities", "entity_spans", "context"],
  "additionalProperties": false,
  "properties": {
    "instruction": {
      "type": "string",
      "description": "Raw user instruction"
    },
    "intent_type": {
      "type": "string",
      "description": "Classified intent type"
    },
    "entities": {
      "type": "object",
      "description": "Extracted entities from the instruction",
      "required": ["software", "version", "project", "file"],
      "additionalProperties": false,
      "properties": {
        "software": {
          "type": ["string", "null"],
          "description": "Target software/runtime/package manager (e.g., python, node, npm)"
        },
        "version": {
          "type": ["string", "null"],
          "description": "Version specifier"
        },
        "project": {
          "type": ["string", "null"],
          "description": "Project or environment name"
        },
        "file": {
          "type": ["string", "null"],
          "description": "File or path reference"
        }
      }
    },
    "entity_spans": {
      "type": "object",
      "description": "Character spans for extracted entities",
      "additionalProperties": false,
      "properties": {
        "software": {
          "oneOf": [
            { "$ref": "#/definitions/span" },
            { "type": "null" }
          ]
        },
        "version": {
          "oneOf": [
            { "$ref": "#/definitions/span" },
            { "type": "null" }
          ]
        },
        "project": {
          "oneOf": [
            { "$ref": "#/definitions/span" },
            { "type": "null" }
          ]
        },
        "file": {
          "oneOf": [
            { "$ref": "#/definitions/span" },
            { "type": "null" }
          ]
        }
      }
    },
    "context": {
      "type": "object",
      "description": "Optional contextual hints",
      "additionalProperties": false,
      "properties": {
        "os_hint": {
          "type": ["string", "null"],
          "description": "Operating system hint"
        },
        "shell_type": {
          "type": ["string", "null"],
          "description": "Shell type hint"
        }
      }
    },
    "paraphrase_group": {
      "type": ["string", "null"],
      "description": "Groups paraphrased variants of the same intent"
    }
  },
  "definitions": {
    "span": {
      "type": "object",
      "required": ["start", "end", "text"],
      "additionalProperties": false,
      "properties": {
        "start": {
          "type": "integer",
          "description": "Start character index (0-based)"
        },
        "end": {
          "type": "integer",
          "description": "End character index (exclusive)"
        },
        "text": {
          "type": "string",
          "description": "Matched text"
        }
      }
    }
  }
}
