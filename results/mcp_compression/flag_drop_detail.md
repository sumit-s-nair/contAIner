# Flag Drop Detail

The following flags were **silently dropped** by the abstractive model (Qwen2.5-0.5B-Instruct):

- **`[pip_install_prose_heavy]`** — dropped flag: `-q`
- **`[npm_install_mixed]`** — dropped flag: `--global`
- **`[maven_add_xml_code]`** — dropped flag: `-DskipTests`

**3 flag failures, 0 version failures** across 18 fixtures.
